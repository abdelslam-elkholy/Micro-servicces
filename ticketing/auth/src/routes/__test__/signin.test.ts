import request from "supertest";
import { app } from "../../app";

it("fails when email that dosen't exist applies", async () => {
  //   await request(app)
  //     .post("/api/users/signin")
  //     .send({ email: "test@test.com", password: "password" })
  //     .expect(404);
});
